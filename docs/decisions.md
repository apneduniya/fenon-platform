# Decision log

Append new entries at the bottom. To change a decision, add a new entry that supersedes the old one and set the old entry's status to `Superseded by #N`.

| # | Date | Decision | Why | Alternatives rejected | Status |
|---|------|----------|-----|-----------------------|--------|
| 1 | 2026-09-23 | Get assets (hero isometric, logo, footer mark, article art) through the Figma MCP, with a budget of about 4 calls. Ask the user for direct node links so no exploration calls are needed, and record responses in `docs/figma-extract.md`. | Highest-fidelity vectors. The Figma free plan has tight MCP limits. | Hand-built SVG from screenshots (lower fidelity); cropping the PNGs (text baked in, no theming). | Active |
| 2 | 2026-09-23 | Fonts: Inter + JetBrains Mono via `next/font/google`. | Closest visual match to the design. Self-hosted by next/font, so there's no layout shift. | Geist + Geist Mono. | Active |
| 3 | 2026-09-23 | `/contact` and `/engineering/[slug]` are real routes. They show as modals from the homepage via `@modal` + `(.)` intercepted routes. | Deep-linkable, shareable, and works with the back button; this is the Next.js documented pattern. | Client-only state-driven dialogs. | Active |
| 4 | 2026-09-23 | Server data is fetched in Server Components. TanStack Query is deferred until client-side fetching is needed. | The Next.js docs make server fetching the default; a landing page has no client data today. | Installing TanStack Query up front. | Active |
| 5 | 2026-09-23 | Zustand is used only for client state shared across components, with the store created per provider. Local UI state uses `useState`. | A module singleton can leak state across server requests (Zustand's Next.js guide). A global store for local state adds coupling. | Zustand for all client state. | Active |
| 6 | 2026-09-23 | The theme's source of truth is the `.dark` class set by an inline pre-hydration script; the Zustand store mirrors it. | Prevents a flash of the wrong theme. `persist` rehydrates after paint. | Zustand `persist` only; next-themes (an extra dependency). | Active |
| 7 | 2026-09-23 | UI primitives: shadcn/ui on Base UI (not Radix), with shadcn CSS variables mapped to Fenon tokens. | User preference. Base UI provides accessible Dialog, Tabs and Select. | Radix-based shadcn; native `<dialog>`. | Active |
| 8 | 2026-09-23 | Forms use TanStack Form + Zod. The contact form builds a `mailto:founders@fenon.ai` draft, with no server submission. | User preference; matches the design copy ("opens an email draft"). A future server path would use a Server Action with the same schema. | react-hook-form; Server Action today. | Active |
| 9 | 2026-09-23 | `content/` data (workloads, articles, site links) is typed and validated with Zod. | Bad copy fails at build time; one source for types. | Plain TS object literals. | Active |
| 10 | 2026-09-23 | All images are local, so `images.remotePatterns` is not configured. | No remote image hosts; smaller attack surface. | — | Active |
| 11 | 2026-09-23 | Animate only `transform`/`opacity` and respect `prefers-reduced-motion`. | GPU compositing, no layout thrash. | Width/height transitions. | Active |
| 12 | 2026-09-23 | Theming has 3 layers: palette primitives (`palette.css`, the only raw colours) → semantic shadcn-style tokens (`tokens.css`, `:root` + `.dark`) → `@theme inline` utilities (`theme.css`). Colours and type sizes are always tokens at exact Figma values. Arbitrary values are only for one-off layout measurements. `lint:tokens` enforces it, and the theme is finished before any component. | One place to change brand colours, no informal colour use, and dark mode for free. Follows the shadcn theming docs. | Arbitrary `text-[15px]` / `bg-[#…]` classes in components. | Active |
| 13 | 2026-09-23 | Section eyebrows drop the "0N /" numeric prefix, a deliberate deviation from the Figma. Tab, step, trace-row and hero-callout numbering is kept. | User request. | Matching the design exactly. | Active |
| 14 | 2026-09-23 | When guidance conflicts, the priority order is Next.js docs → AGENTS.md → user preferences, with conflicts flagged to the user and logged here. | Keeps the framework's best practices authoritative. | — | Active |
| 15 | 2026-09-23 | The theme files live in a root `styles/` folder (`palette.css` → `tokens.css` → `theme.css`), imported by `app/globals.css`. | With Turbopack, relative imports from `app/styles/` failed (`Cannot apply unknown utility class border-border`), but they compile from the root. This also keeps `app/` for routing only. | `app/styles/`; one monolithic `globals.css`. | Active |
| 16 | 2026-09-23 | `lib/brand-colors.ts` is the only TS file allowed to hold colour literals. It mirrors `palette.css` for the viewport `themeColor` and OG images. | Those APIs need literal strings; CSS variables can't reach them. | Hardcoding hex values in `layout.tsx`. | Active |
| 17 | 2026-09-23 | Type tokens are fluid between the 390 and 1440 frames. Size comes from cap height, line height from baseline pitch, weight is 400, and tracking is solved from measured line widths (verified within about 1%). | The Figma file has no font data ("No_Fonts", text outlined), so values had to be measured. Fluid clamps match both frames exactly. | Rounding to Tailwind's default scale; separate breakpoint overrides. | Active |
| 18 | 2026-09-23 | Tailwind's default colour palette is reset (`--color-*: initial`), so only Fenon tokens produce colour utilities. | Makes informal colours impossible to use by accident; `lint:tokens` also guards against them. | Keeping defaults and relying on lint only. | Active |
| 19 | 2026-09-23 | The footer's slightly different greys (`#151713`, `#3C4037`, `#676D5F`, `#D9DDD4`) are unified with the page palette (ink, muted-foreground, border). | The difference is imperceptible and a second grey family would duplicate tokens. | Separate footer tokens. | Active |
| 20 | 2026-09-23 | The footer dot texture is rebuilt as the `bg-dot-grid-pattern` CSS utility (2px / 20px / 47%, `--dot-grid` token) instead of the Figma PNG. | The PNG is a fixed light colour; the CSS version follows the theme at no network cost. | Two PNGs, one per theme. | Active |
| 21 | 2026-09-23 | The hero illustration is used as the exact Figma SVG export, with one file per theme (light `0:7286`, dark `0:580`) and no edits to its paths. | The Figma design-to-code rules forbid editing or inlining exported assets. The SVG has about 80 masked layers and outlined callout text. | A tokenised inline React SVG (would edit the asset). | Active — AGENTS "SVGs use tokens" rule does not apply to Figma illustration exports |
| 22 | 2026-09-23 | The logo is built from the exact logo-system vector paths (crescent, ring, wordmark), with only fills bound to tokens: crescent and wordmark → `currentColor`, ring → `brand-mark`. | The logo system itself defines the ink (light) and white (dark) variants and keeps the orange fixed. One component serves both themes. | Two exported files per theme. | Active |
| 23 | 2026-09-23 | Class merging uses shadcn's `cn` package via `createCn`, with Fenon font-size tokens registered. shadcn is initialised with `--base base --preset nova` (style `base-nova`). | Chosen by shadcn's current CLI. Without registration, `cn()` drops colour classes next to custom `text-*` sizes. | clsx + tailwind-merge. | Active |
| 24 | 2026-09-23 | The hero uses `ThemeImage`: two lazy `next/image` elements swapped with `dark:hidden` / `hidden dark:block`, with `fetchPriority="high"` and no `priority`/`preload`. | `priority` is deprecated in Next 16. The docs' light/dark pattern warns that `preload`/`eager` would download both images. Next's dev-mode LCP hint suggesting `loading="eager"` is knowingly declined. | `loading="eager"` (double download); a single tokenised inline SVG (conflicts with #21). | Active |
| 25 | 2026-09-23 | "Play motion" drives a transform-only float of the hero illustration via the Zustand `motionPlaying` state. It starts paused under `prefers-reduced-motion`. | The exported SVG is static, so animating its internals would require editing the asset (#21). The toggle is kept, as designed. | Animating SVG internals; dropping the toggle. | Active |
| 26 | 2026-09-23 | Code samples are pre-tokenised in `content/code-samples.ts` and coloured with the `syntax-*` tokens; no runtime highlighter. | Two short static snippets; zero JS cost; tokens keep the theme consistent. | Shiki/Prism. | Active |
| 27 | 2026-09-23 | The engineering-note card illustrations are hand-built SVGs (`ArticleArt`) traced from the PNG, with token colours. | They weren't exported from Figma (free-plan budget); they're simple line art. | An extra Figma call. | Active — user chose hand-built (see #38) |
| 28 | 2026-09-23 | `FooterMark` is composed from the exact Figma footer layers (group "02 Brand") with fill vs stroke preserved; guides → `border`, disc → `foreground`, ring → `brand-mark`. | Same approach as the logo (#22): exact geometry, themeable. | Exporting separate light and dark files. | Active |
| 29 | 2026-09-23 | New semantic token `--eyebrow`: section eyebrows are ink in light and orange in dark. | That's what the dark design frames show; the hero eyebrow stays foreground. | Hard-coding `dark:text-primary`. | Active |
| 30 | 2026-09-23 | Workload tabs use Base UI Tabs with `orientation="vertical"` and `activateOnFocus`. On mobile they're laid out as a 3-column row by CSS alone. | Choosing orientation per breakpoint in JS would cause a hydration mismatch. Arrow keys still work on all sizes. | A `matchMedia`-driven orientation. | Active |
| 31 | 2026-09-23 | Fixes to generated shadcn files: `Tabs` now forwards `orientation` to Base UI's root; the Dialog backdrop uses `bg-background/80` instead of `bg-black/10`; `cn` imports are rewritten to `@/lib/utils` (enforced by `lint:tokens`). | The generated code dropped orientation (so arrow keys did nothing) and used palette colours. | Leaving the generated code as-is. | Active — re-apply after any `shadcn add --overwrite` |
| 32 | 2026-09-23 | Contact "Workload" options: Robotics inference (default, from the design), Reasoning & planning, World-model inference, Policies & actions. | The design shows only the default value; the other options reuse the site's own capability labels. | Inventing new categories. | Superseded by #37 |
| 33 | 2026-09-23 | The footer's giant "fenon" wordmark is used at all sizes. The mobile design frame shows an orange dithered pixel artwork (a figure walking) in that slot instead. | That artwork isn't in the exported footer node (0:12888) and likely comes from a separate animated or image layer. | Guessing or redrawing the artwork. | Superseded by #39 |
| 34 | 2026-09-23 | Dev gotcha: files replaced in place (e.g. `sed -i`) sometimes aren't rescanned by Tailwind under Turbopack, leaving stale CSS. Restart `bun dev` if new classes don't apply. | Observed during the build. | — | Note |
| 35 | 2026-09-23 | The contact form's Select hides Base UI's chevron (the design shows none), and inputs use `text-lede-md` (15px). The route modal's initial focus is Base UI's default (the first tabbable element, the close button). | Matches the design measurements; the default focus behaviour is accessible. | A native `<select>`. | Active |
| 36 | 2026-09-23 | Spacing is verified by measuring, not by eye. Rules and card edges come from column scans of the full-resolution PNG vs the render; headings from glyph-band tops; landmarks from DOM rects. Current fit: mobile structural lines within ±1.3px, desktop landmarks within ±2px. | Earlier mobile references were read visually and were off by up to 9px. | Visual comparison only. | Active |
| 37 | 2026-09-23 | The contact Workload select offers a single option, "Robotics inference", exactly as designed. | User decision. | The four-option list (#32). | Active |
| 38 | 2026-09-23 | Article illustrations stay hand-built (user decision) and were refined against the design. Geometry was measured at 4× (the arc is a least-squares fit, max residual 0.11px). The SVG covers the card's border box so design coordinates map 1:1. Remaining overlay differences are sub-pixel anti-aliasing. | Keeps zero extra Figma calls and full theming. | Figma exports. | Active |
| 39 | 2026-09-23 | Below `md`, the footer shows the Figma pixel artwork (light `0:26248`, dark `0:19812`) in place of the wordmark. It's full-bleed at 390×250, sits flush on the bottom bar, and uses unedited PNG exports via `ThemeImage` with `unoptimized` and `image-rendering: pixelated`. From `md` up the wordmark stays. | Matches the 390 and 1440 frames. `unoptimized` stops the image optimiser from resampling (blurring) the dithering. | Optimised next/image output; redrawing it. | Active |
| 40 | 2026-09-23 | The mobile footer is calibrated to the 390 frame: brand cell with absolutely positioned mark (0.551×), wordmark and tagline; 25px column insets; a 173px Connect column; the switch at 126×38. New footer tokens (`mono-ruler`, `mono-artwork`, `mono-bar`, `mono-switch`, `mono-tagline`, fluid `mono-label`) also bring desktop in line with the Figma export (e.g. the bottom bar is 10.3px, not 11.5). All footer landmarks are within ±1px on both frames. | Measured, not eyeballed (#36). | Reusing the generic mono tokens. | Active |
| 41 | 2026-09-23 | `fenon_full_web_design/` (Figma PNG exports) is git-ignored and was removed from the unpushed history before the first push. | User request. The 21 MB of binaries made pushes to GitHub fail and aren't needed to build the site; measurements are captured in `docs/`. | Git LFS; keeping them versioned. | Active |
| 42 | 2026-09-24 | Hosting is Vercel project `fenon-platform` (team apneduniyas-projects), Git-connected to `apneduniya/fenon-platform`: `main` deploys to production at fenon-platform.vercel.app, and other branches get previews. `.vercelignore` excludes `fenon_full_web_design/`. | User request; Git-based deploys build exactly what's committed. | Manual `vercel --prod` uploads from a local working tree. | Active |
